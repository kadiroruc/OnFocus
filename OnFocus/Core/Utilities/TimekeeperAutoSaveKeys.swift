import Foundation

enum TimekeeperAutoSaveKeys {
    static let pendingFlag = "timekeeper.pending_auto_save"
    static let elapsedSeconds = "timekeeper.pending_elapsed_seconds"
    static let timestamp = "timekeeper.pending_timestamp"
    static let pausedElapsedSeconds = "timekeeper.paused_elapsed_seconds"
}
